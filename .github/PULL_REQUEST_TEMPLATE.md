## What

<!-- What does this PR change, in one or two sentences? -->

## Why

<!-- Link the issue if there is one: Fixes #123 -->

## How was it tested?

- [ ] `xcodebuild -scheme MrMark -destination 'platform=macOS' test` passes
- [ ] `swiftformat --lint .` passes
- [ ] Manual test steps (for UI changes):

## Performance

<!-- If this touches the launch path, the viewer, or per-keystroke work:
     how did you confirm the budgets still hold?
     (<200ms launch to viewer, <100MB per window, no full re-render per keystroke) -->
