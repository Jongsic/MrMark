# MrMark for Windows

Reserved for the native Windows app. Not started yet — the macOS app in
[`macos/`](../macos) is the shipping product today. The Windows version will
be a separate native implementation sharing the product spec, not code.
